name: Defold Android TV Smoke Manual

on:
  workflow_dispatch:

permissions:
  contents: write

jobs:
  note:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Android TV smoke now runs automatically as the second job in Defold Prototype Build."
